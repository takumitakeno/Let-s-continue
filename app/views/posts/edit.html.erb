<div class="container">
  <div class="row">
	<%= render 'users/profire', user: current_user %>
	<div class="col-xs-8">
	  <div class="panel panel-default">
	    <div class="panel-body">
		  <%= attachment_image_tag @post.user, :profile_image, format: 'jpeg', size: "100x100", class: "img-circle" %>
		  <%= @post.user.user_name %>
	    </div>
	    <div class="panel-body">
	      <%= form_for (@post) do |f| %>
	      <h4>トレーニング画像</h4>
	        <%= f.attachment_field :training_image %><br>
	        <%= f.text_area :post, class: "form-control", size: "10x10" %><br>
	        <%= f.text_field :tag_list, value: @post.tag_list.join(','), class: "form-control" %><br>
	        <%= f.submit "編集する", class: "btn btn-default" %>
	      <% end %>
	    </div>
	  </div>
	</div>
  </div>
</div>